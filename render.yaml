services:
  - type: web
    name: tools-status-serverBasedAI
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.12
      - key: DISABLE_PORT_CHECKS
        value: true